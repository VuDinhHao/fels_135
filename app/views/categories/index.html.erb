<% provide :title, t("views.categories.index.allcategories") %>
<h1> <%= t "views.categories.index.allcategories" %> </h1>
<ul class "categories">
  <% @categories.each do |category| %>
    <li>
      <%=link_to t("views.categories.index.startlesson"), "#", class: "btn btn-lg btn-primary" %>
      <%=  category.name %>
    </li>
  <% end %>
</ul>
